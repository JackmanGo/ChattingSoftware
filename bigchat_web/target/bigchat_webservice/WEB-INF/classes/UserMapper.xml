<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">
	<insert id="save" keyProperty="user_id"
     parameterType="domain.User">
		insert into user (user_id,user_password) values(#{user_id},#{user_password})
	</insert>
	<select id="login" resultType="domain.User" parameterType="domain.User">
        select * from user where user_id=#{user_id} and user_password=#{user_password}
	</select>
	<select id="findAll" resultType="domain.User">
		 select * from user
	</select>
	<select id="findById" resultType="domain.User" parameterType="String">
	     select * from user where user_id = #{user_id}
	</select>
		<update id="setUser_name"  parameterType="domain.User">
	      update user set user_name = #{user_name} where user_id = #{user_id}
	</update>
	<delete id="delete"  parameterType="integer">
	      delete from user where id=#{pid}
	</delete>
		<select id="sum" resultType="integer" parameterType="integer">
	      select count(*) from user
	</select>
</mapper>